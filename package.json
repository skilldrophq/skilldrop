{
  "name": "skilldrop",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "cli"
  ],
  "scripts": {
    "build:cli": "bun run --cwd cli build",
    "test:cli": "bun run --cwd cli test",
    "check:cli": "bun run --cwd cli check"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
