{
  "name": "skilldrop",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:cli": "bun run --cwd packages/cli build",
    "test:cli": "bun run --cwd packages/cli test",
    "check:cli": "bun run --cwd packages/cli check"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
